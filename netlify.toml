const STORAGE_KEY = "hero_checkup_v1";
const WHATSAPP_NUMBER = "5511924787933";

const state = {
  phase: "landing",
  route: "landing",
  profile: null,
  current: 0,
  leadCaptured: false,
  answers: {},
  lead: {},
  result: null,
};

const integrations = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  webhookUrl: "",
};

const dimensions = [
  "Energia",
  "Composição corporal",
  "Força",
  "Recuperação",
  "Consistência",
  "Confiança corporal",
  "Capacidade de evolução",
];

const evidenceCards = {
  activity: {
    male: {
      stat: "29%",
      title: "dos homens no mundo não atingem o nível recomendado de atividade física.",
      note: "A OMS recomenda 150 minutos de atividade moderada por semana. A pergunta existe porque performance começa na regularidade.",
      source: "OMS, 2024",
      url: "https://www.who.int/news/item/26-06-2024-nearly-1.8-billion-adults-at-risk-of-disease-from-not-doing-enough-physical-activity",
      image: "./assets/evidence-male-activity.png",
    },
    female: {
      stat: "34%",
      title: "das mulheres no mundo não atingem o nível recomendado de atividade física.",
      note: "A OMS aponta que a inatividade é mais comum entre mulheres globalmente, o que torna rotina e acesso pontos decisivos.",
      source: "OMS, 2024",
      url: "https://www.who.int/news/item/26-06-2024-nearly-1.8-billion-adults-at-risk-of-disease-from-not-doing-enough-physical-activity",
      image: "./assets/evidence-female-activity.png",
    },
  },
  sleep: {
    male: {
      stat: "37%",
      title: "dos homens adultos nos EUA relataram sono insuficiente em 2022.",
      note: "O CDC classifica menos de 7 horas por noite como sono insuficiente. Energia e recuperação dependem desse sinal básico.",
      source: "CDC, 2024",
      url: "https://www.cdc.gov/sleep/data-research/facts-stats/adults-sleep-facts-and-stats.html",
      image: "./assets/evidence-male-sleep.png",
    },
    female: {
      stat: "7h",
      title: "é o mínimo recomendado de sono para adultos, segundo o CDC.",
      note: "Quando a rotina acumula cuidado, trabalho e carga mental, o sono costuma ser o primeiro marcador a mostrar desgaste.",
      source: "CDC, 2024",
      url: "https://www.cdc.gov/sleep/data-research/facts-stats/adults-sleep-facts-and-stats.html",
      image: "./assets/evidence-female-sleep.png",
    },
  },
  body: {
    male: {
      stat: "57,5%",
      title: "dos homens adultos brasileiros estavam com sobrepeso em 2019.",
      note: "A PNS/IBGE também encontrou obesidade em 21,8% dos homens. Composição corporal é um sinal de rotina acumulada.",
      source: "IBGE/PNS, 2019",
      url: "https://agenciadenoticias.ibge.gov.br/agencia-noticias/2012-agencia-de-noticias/noticias/29204-um-em-cada-quatro-adultos-do-pais-estava-obeso-em-2019",
      image: "./assets/evidence-male-body.png",
    },
    female: {
      stat: "62,6%",
      title: "das mulheres adultas brasileiras estavam com sobrepeso em 2019.",
      note: "A PNS/IBGE também encontrou obesidade em 29,5% das mulheres. O ponto aqui não é culpa; é clareza sobre direção.",
      source: "IBGE/PNS, 2019",
      url: "https://agenciadenoticias.ibge.gov.br/agencia-noticias/2012-agencia-de-noticias/noticias/29204-um-em-cada-quatro-adultos-do-pais-estava-obeso-em-2019",
      image: "./assets/evidence-female-body.png",
    },
  },
  consistency: {
    male: {
      stat: "35%",
      title: "é a projeção de inatividade global para 2030 se a tendência continuar.",
      note: "Para homens com agenda exigente, o risco costuma aparecer como desempenho mental alto e corpo sempre em segundo plano.",
      source: "OMS, 2024",
      url: "https://www.who.int/news/item/26-06-2024-nearly-1.8-billion-adults-at-risk-of-disease-from-not-doing-enough-physical-activity",
      image: "./assets/evidence-male-consistency.png",
    },
    female: {
      stat: "35%",
      title: "é a projeção de inatividade global para 2030 se a tendência continuar.",
      note: "Para mulheres, rotina, sobrecarga e tempo disponível são parte da pergunta. Consistência precisa caber na vida real.",
      source: "OMS, 2024",
      url: "https://www.who.int/news/item/26-06-2024-nearly-1.8-billion-adults-at-risk-of-disease-from-not-doing-enough-physical-activity",
      image: "./assets/evidence-female-consistency.png",
    },
  },
};

const intro = {
  id: "intro",
  kind: "intro",
  label: "Antes de começar",
  title: "Este check-up não mede perfeição. Mede direção.",
  support:
    "Responda com honestidade. Em menos de 3 minutos, vamos organizar sinais da sua rotina, energia, corpo e capacidade de evolução em um diagnóstico simples de entender.",
};

const questions = [
  {
    id: "profile",
    label: "Personalização",
    title: "Para personalizarmos sua análise, escolha seu perfil:",
    options: [
      {
        text: "Homem",
        detail: "Quero avaliar minha energia, força, disposição, composição corporal e performance.",
        value: "male",
        score: 0,
      },
      {
        text: "Mulher",
        detail: "Quero avaliar minha energia, autoestima, composição corporal, rotina e evolução física.",
        value: "female",
        score: 0,
      },
    ],
  },
  {
    id: "body_feeling",
    label: "Identificação",
    title: "Quando foi a última vez que você realmente se sentiu bem com seu corpo?",
    evidence: "body",
    options: [
      { text: "Ainda me sinto bem na maior parte do tempo.", score: 92 },
      { text: "Tive bons períodos recentemente, mas não consigo manter.", score: 68 },
      { text: "Faz meses que eu sinto que perdi o controle.", score: 42 },
      { text: "Faz tanto tempo que quase não lembro dessa sensação.", score: 18 },
    ],
  },
  {
    id: "wake_up",
    label: "Energia",
    kind: "scale",
    title: "De 0 a 10, quanta energia real você sente ao acordar?",
    support: "Pense nos últimos 14 dias, não em um dia isolado.",
    evidence: "sleep",
    minLabel: "Acordo arrastado",
    maxLabel: "Acordo pronto",
    scoreMap: "direct",
  },
  {
    id: "wake_up_pattern",
    label: "Energia",
    title: "Como você acorda na maioria dos dias?",
    evidence: "sleep",
    options: [
      { text: "Com energia suficiente para começar bem.", score: 88 },
      { text: "Funcional, mas sem aquela disposição real.", score: 62 },
      { text: "Cansado, mesmo dormindo algumas horas.", score: 36 },
      { text: "Arrastado, como se o dia já começasse pesado.", score: 16 },
    ],
  },
  {
    id: "routine",
    label: "Rotina",
    title: "Hoje, qual frase mais representa sua rotina?",
    evidence: "consistency",
    options: [
      { text: "Tenho uma rotina exigente, mas ainda consigo me cuidar.", score: 84 },
      { text: "Eu até tento, mas minha consistência oscila demais.", score: 58 },
      { text: "Minha agenda sempre engole meu treino e minha alimentação.", score: 32 },
      { text: "Eu vivo apagando incêndios e deixo meu corpo por último.", score: 14 },
    ],
  },
  {
    id: "mirror",
    label: "Desconforto",
    title: "Quando você se olha no espelho, como se sente?",
    evidence: "body",
    options: [
      { text: "Reconheço meu esforço e sei onde quero melhorar.", score: 86 },
      { text: "Vejo potencial, mas também muita frustração.", score: 58 },
      { text: "Evito olhar com atenção porque me incomoda.", score: 30 },
      { text: "Sinto que me afastei muito da pessoa que eu queria ser.", score: 12 },
    ],
  },
  {
    id: "open_frustration",
    label: "Resposta aberta",
    kind: "text",
    title: "Se você pudesse mudar uma coisa no seu corpo ou na sua energia nos próximos 90 dias, o que seria?",
    support: "Escreva do seu jeito. Essa resposta entra no seu mapa final e ajuda a personalizar a conversa com o time HERO.",
    placeholder: "Ex: voltar a ter disposição, perder gordura abdominal, recuperar força, parar de recomeçar...",
    minLength: 8,
    score: 52,
    tag: "dor_aberta",
  },
  {
    id: "attempts",
    label: "Histórico",
    title: "Quantas vezes você tentou mudar o corpo no último ano?",
    options: [
      { text: "Mantive um processo consistente.", score: 88 },
      { text: "Tentei uma ou duas vezes, mas perdi ritmo.", score: 56 },
      { text: "Comecei várias vezes e parei várias vezes.", score: 30 },
      { text: "Nem consegui começar de verdade.", score: 16 },
    ],
  },
  {
    id: "blocker",
    label: "Bloqueio",
    title: "O que mais atrapalha você?",
    evidence: "activity",
    options: [
      { text: "Falta de clareza sobre o que fazer.", score: 48, tag: "clareza" },
      { text: "Falta de tempo e excesso de responsabilidade.", score: 42, tag: "tempo" },
      { text: "Cansaço, baixa energia e pouca recuperação.", score: 30, tag: "energia" },
      { text: "Falta de acompanhamento e cobrança inteligente.", score: 46, tag: "acompanhamento" },
    ],
  },
  {
    id: "m_energy",
    profile: "male",
    label: "Energia masculina",
    title: "Em relação à sua energia ao longo do dia, qual cenário é mais real?",
    evidence: "sleep",
    options: [
      { text: "Tenho boa energia e consigo sustentar meu ritmo.", score: 86 },
      { text: "Tenho picos bons, mas caio muito no fim do dia.", score: 60 },
      { text: "Uso cafeína ou força mental para funcionar.", score: 34 },
      { text: "Sinto que minha energia caiu de forma evidente.", score: 16 },
    ],
  },
  {
    id: "m_composition",
    profile: "male",
    label: "Composição corporal",
    title: "Sobre gordura, massa muscular e definição, o que melhor descreve você?",
    evidence: "body",
    options: [
      { text: "Estou perto do corpo que quero construir.", score: 84 },
      { text: "Ganhei gordura ou perdi definição, mas ainda tenho base.", score: 58 },
      { text: "Perdi massa, força e forma com o tempo.", score: 30 },
      { text: "Meu corpo parece não responder mais como antes.", score: 14 },
    ],
  },
  {
    id: "m_recovery",
    profile: "male",
    label: "Recuperação",
    title: "Como seu corpo responde depois de esforço, treino ou dias intensos?",
    options: [
      { text: "Recupero bem e volto pronto.", score: 86 },
      { text: "Demoro um pouco, mas consigo manter.", score: 62 },
      { text: "Fico dolorido, travado ou sem energia por dias.", score: 34 },
      { text: "Evito esforço porque sei que vou pagar caro depois.", score: 14 },
    ],
  },
  {
    id: "m_strength",
    profile: "male",
    label: "Força",
    title: "Como está sua percepção de força hoje?",
    evidence: "activity",
    options: [
      { text: "Sinto força e evolução nos movimentos.", score: 88 },
      { text: "Tenho força, mas estou abaixo do meu potencial.", score: 62 },
      { text: "Percebo queda de força em tarefas simples e treinos.", score: 34 },
      { text: "Sinto que meu corpo perdeu presença e resposta.", score: 16 },
    ],
  },
  {
    id: "m_confidence",
    profile: "male",
    label: "Confiança",
    title: "O quanto seu corpo influencia sua confiança hoje?",
    options: [
      { text: "Meu corpo reforça minha segurança.", score: 86 },
      { text: "Às vezes incomoda, mas não domina minha confiança.", score: 60 },
      { text: "Afeta minha postura, roupas e autoestima.", score: 32 },
      { text: "Eu sinto que perdi parte da minha identidade.", score: 12 },
    ],
  },
  {
    id: "m_power",
    profile: "male",
    label: "Potência",
    title: "Sem entrar em detalhes íntimos: sua vitalidade e potência parecem estar em que nível?",
    support:
      "Essa pergunta observa sinais de vitalidade geral. Não é uma avaliação clínica, hormonal ou médica.",
    options: [
      { text: "Boa, estável e coerente com minha fase de vida.", score: 86 },
      { text: "Oscila mais do que antes.", score: 58 },
      { text: "Caiu junto com meu cansaço e minha rotina.", score: 30 },
      { text: "É uma preocupação clara para mim hoje.", score: 14 },
    ],
  },
  {
    id: "f_overload",
    profile: "female",
    label: "Sobrecarga",
    title: "Qual frase descreve melhor sua carga mental e física hoje?",
    evidence: "consistency",
    options: [
      { text: "Tenho responsabilidades, mas ainda encontro espaço para mim.", score: 84 },
      { text: "Cuido de tudo, mas meu autocuidado oscila muito.", score: 58 },
      { text: "Sinto que estou sempre devendo algo para mim.", score: 32 },
      { text: "Estou no limite e meu corpo já está cobrando.", score: 14 },
    ],
  },
  {
    id: "f_energy",
    profile: "female",
    label: "Energia feminina",
    title: "Como sua energia costuma se comportar ao longo da semana?",
    options: [
      { text: "Tenho energia estável para viver e treinar.", score: 86 },
      { text: "Oscilo, mas ainda consigo encaixar movimento.", score: 60 },
      { text: "Tenho mais cansaço do que disposição.", score: 34 },
      { text: "Sinto que estou funcionando no automático.", score: 14 },
    ],
  },
  {
    id: "f_body",
    profile: "female",
    label: "Corpo",
    title: "Como você percebe sua composição corporal hoje?",
    evidence: "body",
    options: [
      { text: "Estou construindo uma relação boa com meu corpo.", score: 84 },
      { text: "Vejo pontos a melhorar, mas ainda me reconheço.", score: 60 },
      { text: "Sinto acúmulo, perda de tônus ou mudanças que incomodam.", score: 32 },
      { text: "Parece que meu corpo mudou e eu não consegui acompanhar.", score: 14 },
    ],
  },
  {
    id: "f_identity",
    profile: "female",
    label: "Identidade",
    title: "O quanto sua rotina atual parece ter afastado você da sua própria identidade?",
    options: [
      { text: "Pouco. Ainda consigo me priorizar.", score: 86 },
      { text: "Às vezes sinto que me deixo para depois.", score: 58 },
      { text: "Com frequência sinto que virei apenas função e obrigação.", score: 30 },
      { text: "Sinto saudade da mulher que eu era.", score: 12 },
    ],
  },
  {
    id: "f_selfesteem",
    profile: "female",
    label: "Autoestima",
    title: "Como sua autoestima corporal aparece na vida real?",
    evidence: "body",
    options: [
      { text: "Escolho roupas e ambientes com segurança.", score: 86 },
      { text: "Tenho dias bons, mas ainda me comparo bastante.", score: 58 },
      { text: "Evito certas roupas, fotos ou situações.", score: 30 },
      { text: "Meu corpo virou uma fonte constante de cobrança.", score: 12 },
    ],
  },
  {
    id: "f_recovery",
    profile: "female",
    label: "Recuperação",
    title: "Considerando ciclo, fase de vida e rotina, como está sua recuperação?",
    support:
      "Essa pergunta considera rotina, sono, energia e percepção corporal. Não substitui avaliação médica ou ginecológica.",
    options: [
      { text: "Boa e previsível na maior parte do tempo.", score: 86 },
      { text: "Oscila, mas consigo ajustar quando preciso.", score: 60 },
      { text: "Sinto inchaço, cansaço ou queda de rendimento frequentes.", score: 32 },
      { text: "Meu corpo parece pedir pausa o tempo todo.", score: 14 },
    ],
  },
  {
    id: "goal_main",
    label: "Objetivo",
    title: "Qual resultado mais mudaria sua vida hoje?",
    evidence: "activity",
    options: [
      { text: "Ter energia e disposição de novo.", score: 56, tag: "energia" },
      { text: "Reduzir gordura e melhorar minha composição corporal.", score: 54, tag: "composicao" },
      { text: "Ganhar força, forma e confiança.", score: 58, tag: "forca" },
      { text: "Voltar a ter consistência sem depender de motivação.", score: 50, tag: "consistencia" },
    ],
  },
  {
    id: "time_training",
    label: "Tempo",
    title: "Quanto tempo você consegue dedicar por treino?",
    options: [
      { text: "Até 30 minutos, desde que seja objetivo.", score: 54 },
      { text: "Entre 35 e 50 minutos.", score: 70 },
      { text: "Até 60 minutos quando a rotina permite.", score: 78 },
      { text: "Não sei. Preciso de algo adaptado à minha semana.", score: 46 },
    ],
  },
  {
    id: "days_training",
    label: "Frequência",
    kind: "scale",
    title: "Em uma semana real, quantos dias você conseguiria treinar de verdade?",
    support: "Arraste para escolher uma meta honesta. O melhor plano é aquele que sobrevive à sua rotina.",
    evidence: "activity",
    minLabel: "0 dias",
    maxLabel: "6 dias",
    max: 6,
    scoreMap: "trainingDays",
  },
  {
    id: "days_training_choice",
    label: "Frequência",
    title: "Qual frase combina melhor com essa frequência?",
    options: [
      { text: "2 dias bem feitos.", score: 52 },
      { text: "3 dias consistentes.", score: 72 },
      { text: "4 ou mais dias, com organização.", score: 86 },
      { text: "Hoje eu não sei nem por onde começar.", score: 30 },
    ],
  },
  {
    id: "support_need",
    label: "Continuidade",
    title: "O que você mais precisa para conseguir continuar?",
    options: [
      { text: "Um plano claro e realista.", score: 54, tag: "plano" },
      { text: "Acompanhamento humano e ajustes semanais.", score: 58, tag: "acompanhamento" },
      { text: "Treinos que encaixem na minha rotina.", score: 56, tag: "rotina" },
      { text: "Cobrança inteligente para não abandonar.", score: 50, tag: "cobranca" },
    ],
  },
  {
    id: "open_commitment",
    label: "Resposta aberta",
    kind: "text",
    title: "O que precisa acontecer para você não abandonar dessa vez?",
    support: "Pode ser cobrança, horário, plano, acompanhamento, ambiente ou uma decisão pessoal.",
    placeholder: "Ex: ter alguém acompanhando, treinos curtos, metas semanais, parar de tentar sozinho...",
    minLength: 8,
    score: 54,
    tag: "continuidade_aberta",
  },
];

const processingMessages = [
  "Analisando seu nível de energia...",
  "Cruzando sua rotina com seu objetivo...",
  "Avaliando seu risco de abandono...",
  "Identificando seus principais bloqueios...",
  "Construindo seu Índice HERO...",
];

const screen = document.querySelector("#screen");
const toast = document.querySelector("#toast");

document.addEventListener("click", (event) => {
  const target = event.target.closest("[data-action], [data-legal]");
  if (!target) return;

  const action = target.dataset.action;
  const legal = target.dataset.legal;

  if (action === "home") resetToLanding();
  if (action === "admin") renderAdmin();
  if (legal) renderLegal(legal);
});

window.addEventListener("popstate", () => {
  if (location.search.includes("admin")) renderAdmin();
});

function boot() {
  if (location.search.includes("admin")) {
    renderAdmin();
    return;
  }
  renderLanding();
}

function renderLanding() {
  state.route = "landing";
  screen.innerHTML = `
    <section class="hero">
      <div class="hero-grid">
        <div class="hero-content">
          <p class="eyebrow">Check-up de Performance HERO</p>
          <h1>Seu corpo está envelhecendo ou apenas pedindo ajuda?</h1>
          <p class="lead">
            Você trabalha, resolve problemas, cuida de outras pessoas e continua cumprindo suas obrigações.
            Mas talvez esteja se acostumando a viver cansado, sem disposição e cada vez menos satisfeito com o
            próprio corpo.
          </p>
          <ul class="trust-list" aria-label="Informações do check-up">
            <li>Gratuito</li>
            <li>Resultado individual</li>
            <li>Menos de 3 minutos</li>
            <li>Análise inteligente</li>
          </ul>
        </div>

        <aside class="access-card" aria-label="Liberar acesso ao quiz">
          <p class="question-label">Liberar acesso</p>
          <h2>Comece seu check-up.</h2>
          <p class="support">
            Preencha seus dados para abrir o quiz e receber um diagnóstico individual ao final.
          </p>
          <form class="form-grid compact" id="landingLeadForm">
            <div class="field">
              <label for="landingFirstName">Primeiro nome</label>
              <input id="landingFirstName" name="firstName" autocomplete="given-name" required />
            </div>
            <div class="field">
              <label for="landingWhatsapp">WhatsApp</label>
              <input id="landingWhatsapp" name="whatsapp" inputmode="tel" autocomplete="tel" placeholder="(00) 00000-0000" required />
            </div>
            <div class="field">
              <label for="landingEmail">E-mail</label>
              <input id="landingEmail" name="email" type="email" autocomplete="email" required />
            </div>
            <label class="check">
              <input id="landingConsent" name="consent" type="checkbox" required />
              <span>Concordo em receber meu resultado e orientações relacionadas à minha performance.</span>
            </label>
            <button class="button primary full" type="submit">LIBERAR MEU QUIZ</button>
            <button class="ghost-inline" type="button" id="seePrivacy">Como seus dados são usados</button>
          </form>
        </aside>
      </div>
    </section>
  `;
  document.querySelector("#landingLeadForm").addEventListener("submit", handleLandingLead);
  document.querySelector("#seePrivacy").addEventListener("click", () => renderLegal("privacy"));
}

function handleLandingLead(event) {
  event.preventDefault();
  const form = new FormData(event.currentTarget);
  state.lead = {
    firstName: String(form.get("firstName")).trim(),
    whatsapp: String(form.get("whatsapp")).trim(),
    email: String(form.get("email")).trim(),
    consent: Boolean(form.get("consent")),
  };

  if (!state.lead.firstName || !state.lead.whatsapp || !state.lead.email || !state.lead.consent) {
    showToast("Preencha seus dados para liberar o quiz.");
    return;
  }

  state.leadCaptured = true;
  persistEvent("quiz_access_released", { stage: "landing" });
  startQuiz();
}

function startQuiz() {
  state.phase = "quiz";
  state.current = -1;
  state.profile = null;
  state.answers = {};
  state.result = null;
  renderIntro();
}

function renderIntro() {
  screen.innerHTML = `
    <section class="workspace">
      <div class="quiz-frame">
        ${progressMarkup(0, "Preparação")}
        <article class="panel">
          <p class="question-label">${intro.label}</p>
          <h2>${intro.title}</h2>
          <p class="support">${intro.support}</p>
          <div class="cta-row">
            <button class="button primary" type="button" id="continueIntro">COMEÇAR AGORA</button>
          </div>
          <p class="micro">Não usamos este check-up como diagnóstico médico. Ele organiza sinais de rotina e performance para orientar o próximo passo.</p>
        </article>
      </div>
    </section>
  `;
  document.querySelector("#continueIntro").addEventListener("click", () => {
    state.current = 0;
    renderQuestion();
  });
}

function getActiveQuestions() {
  return questions.filter((question) => !question.profile || question.profile === state.profile);
}

function renderQuestion() {
  const activeQuestions = getActiveQuestions();
  const question = activeQuestions[state.current];

  if (!question) {
    renderProcessing();
    return;
  }

  const answer = state.answers[question.id] || null;
  const selected = answer?.value ?? null;
  const canContinue = Boolean(answer) || question.kind === "scale";
  screen.innerHTML = `
    <section class="workspace">
      <div class="quiz-frame">
        ${progressMarkup(state.current + 1, question.label)}
        <div class="quiz-stage">
          ${evidenceMarkup(question)}
          <article class="question-card">
            <p class="question-label">${question.label}</p>
            <h2>${question.title}</h2>
            ${question.support ? `<p class="support">${question.support}</p>` : ""}
            ${questionInputMarkup(question, selected, answer)}
            ${answer ? `<div class="answer-feedback">Resposta registrada. O próximo bloco ajusta o seu mapa HERO.</div>` : ""}
            <div class="nav-row">
              <button class="button secondary" type="button" id="backQuestion">Voltar</button>
              ${question.options ? "" : `<button class="button primary" type="button" id="nextQuestion" ${!canContinue ? "disabled" : ""}>Continuar</button>`}
            </div>
          </article>
        </div>
      </div>
    </section>
  `;

  document.querySelectorAll(".option").forEach((button) => {
    button.addEventListener("click", () => {
      const index = Number(button.dataset.index);
      const option = question.options[index];
      const previousProfile = state.profile;
      state.answers[question.id] = {
        question: question.title,
        answer: option.text,
        score: option.score,
        tag: option.tag || null,
        value: option.value ?? index,
      };
      if (question.id === "profile") {
        state.profile = option.value;
        if (previousProfile && previousProfile !== option.value) {
          clearAnswersFromOtherProfile(option.value);
        }
      }
      window.setTimeout(() => {
        state.current += 1;
        renderQuestion();
      }, 180);
    });
  });

  const scaleInput = document.querySelector("#scaleAnswer");
  if (scaleInput) {
    const valueLabel = document.querySelector("#scaleValue");
    scaleInput.addEventListener("input", () => {
      valueLabel.textContent = scaleInput.value;
    });
    scaleInput.addEventListener("change", () => {
      const value = Number(scaleInput.value);
      const max = Number(question.max || 10);
      state.answers[question.id] = {
        question: question.title,
        answer: `${value}/${max}`,
        score: scaleScore(question, value),
        tag: question.tag || null,
        value,
      };
      renderQuestion();
    });
  }

  const textInput = document.querySelector("#textAnswer");
  if (textInput) {
    textInput.addEventListener("input", () => {
      const text = textInput.value.trim();
      const minLength = question.minLength || 3;
      if (text.length >= minLength) {
        state.answers[question.id] = {
          question: question.title,
          answer: text,
          score: question.score || 50,
          tag: question.tag || null,
          value: text,
        };
      } else {
        delete state.answers[question.id];
      }
      document.querySelector("#nextQuestion").disabled = text.length < minLength;
    });
  }

  document.querySelector("#backQuestion").addEventListener("click", goBack);
  const nextButton = document.querySelector("#nextQuestion");
  if (nextButton) {
    nextButton.addEventListener("click", () => {
      if (question.kind === "scale" && !state.answers[question.id]) {
        const scale = document.querySelector("#scaleAnswer");
        const value = Number(scale.value);
        const max = Number(question.max || 10);
        state.answers[question.id] = {
          question: question.title,
          answer: `${value}/${max}`,
          score: scaleScore(question, value),
          tag: question.tag || null,
          value,
        };
      }
      state.current += 1;
      renderQuestion();
    });
  }
}

function questionInputMarkup(question, selected, answer) {
  if (question.kind === "scale") {
    const max = question.max || 10;
    const value = answer?.value ?? Math.round(max / 2);
    return `
      <div class="scale-card">
        <div class="scale-value"><strong id="scaleValue">${value}</strong><span>/${max}</span></div>
        <input id="scaleAnswer" type="range" min="0" max="${max}" step="1" value="${value}" aria-label="${question.title}" />
        <div class="scale-labels">
          <span>${question.minLabel || "Baixo"}</span>
          <span>${question.maxLabel || "Alto"}</span>
        </div>
      </div>
    `;
  }

  if (question.kind === "text") {
    return `
      <div class="open-answer">
        <textarea id="textAnswer" rows="5" maxlength="280" placeholder="${question.placeholder || ""}">${answer?.answer || ""}</textarea>
        <p class="micro">Resposta aberta. Mínimo de ${question.minLength || 3} caracteres.</p>
      </div>
    `;
  }

  return `
    <div class="options" role="radiogroup" aria-label="${question.title}">
      ${question.options
        .map((option, index) => optionMarkup(question, option, index, selected))
        .join("")}
    </div>
  `;
}

function clearAnswersFromOtherProfile(profile) {
  Object.keys(state.answers).forEach((answerId) => {
    const matchedQuestion = questions.find((item) => item.id === answerId);
    if (matchedQuestion?.profile && matchedQuestion.profile !== profile) {
      delete state.answers[answerId];
    }
  });
}

function optionMarkup(question, option, index, selected) {
  const value = option.value ?? index;
  const isSelected = selected === value;
  return `
    <button class="option ${isSelected ? "selected" : ""}" type="button" data-index="${index}" role="radio" aria-checked="${isSelected}">
      <span class="option-dot" aria-hidden="true"></span>
      <span class="option-text">
        <strong>${option.text}</strong>
        ${option.detail ? `<small>${option.detail}</small>` : ""}
      </span>
    </button>
  `;
}

function evidenceMarkup(question) {
  if (question.id === "profile") return "";
  const group = evidenceCards[question.evidence] || evidenceCards.activity;
  const profile = state.profile === "female" ? "female" : "male";
  const card = group[profile];
  return `
    <aside class="evidence-card evidence-type-${question.evidence || "activity"}">
      <img src="${card.image}" alt="" loading="lazy" />
      <div class="evidence-body">
        <span class="evidence-kicker">Dado real</span>
        <strong>${card.stat}</strong>
        <p>${card.title}</p>
        <small>${card.note}</small>
        <a href="${card.url}" target="_blank" rel="noreferrer">${card.source}</a>
      </div>
    </aside>
  `;
}

function scaleScore(question, value) {
  if (question.scoreMap === "trainingDays") {
    return clamp(Math.round((value / Number(question.max || 6)) * 78 + 12), 0, 100);
  }
  return clamp(value * 10, 0, 100);
}

function goBack() {
  if (state.current <= 0) {
    renderIntro();
    return;
  }
  state.current -= 1;
  renderQuestion();
}

function progressMarkup(step, label) {
  const total = getActiveQuestions().length || 1;
  const pct = Math.min(100, Math.round((step / total) * 100));
  return `
    <div class="progress-wrap" aria-label="Progresso do quiz">
      <div class="progress-meta">
        <span>${label}</span>
        <span>${pct}% concluído</span>
      </div>
      <div class="progress-track"><div class="progress-fill" style="width: ${pct}%"></div></div>
    </div>
  `;
}

function renderLeadCapture(isMidpoint) {
  const preview = calculateResult(true);
  screen.innerHTML = `
    <section class="workspace">
      <div class="quiz-frame">
        ${progressMarkup(state.current + 1, "Prévia liberada")}
        <article class="panel">
          <p class="question-label">Clareza</p>
          <h2>Seu Check-up foi concluído.</h2>
          <p class="support">
            Já identificamos sinais importantes sobre sua energia, rotina, composição corporal e capacidade de evolução.
            Preencha os dados abaixo para liberar gratuitamente seu diagnóstico completo.
          </p>
          <div class="offer-band">
            <strong>Prévia do seu Índice HERO: ${preview.overall}/100</strong>
            <p class="micro">${preview.classification.description}</p>
          </div>
          <form class="form-grid" id="leadForm">
            <div class="field">
              <label for="firstName">Primeiro nome</label>
              <input id="firstName" name="firstName" autocomplete="given-name" required />
            </div>
            <div class="field">
              <label for="whatsapp">WhatsApp</label>
              <input id="whatsapp" name="whatsapp" inputmode="tel" autocomplete="tel" placeholder="(00) 00000-0000" required />
            </div>
            <div class="field">
              <label for="email">E-mail</label>
              <input id="email" name="email" type="email" autocomplete="email" required />
            </div>
            <label class="check">
              <input id="consent" name="consent" type="checkbox" required />
              <span>Concordo em receber meu resultado e orientações relacionadas à minha performance.</span>
            </label>
            <button class="button primary full" type="submit">LIBERAR MEU RESULTADO</button>
            <p class="micro">
              Seus dados serão utilizados apenas para enviar seu diagnóstico e conteúdos relacionados à sua evolução.
            </p>
          </form>
        </article>
      </div>
    </section>
  `;

  document.querySelector("#leadForm").addEventListener("submit", (event) => {
    event.preventDefault();
    const form = new FormData(event.currentTarget);
    state.lead = {
      firstName: String(form.get("firstName")).trim(),
      whatsapp: String(form.get("whatsapp")).trim(),
      email: String(form.get("email")).trim(),
      consent: Boolean(form.get("consent")),
    };

    if (!state.lead.firstName || !state.lead.whatsapp || !state.lead.email || !state.lead.consent) {
      showToast("Preencha os dados e confirme o consentimento para continuar.");
      return;
    }

    state.leadCaptured = true;
    persistEvent("lead_captured", preview);
    showToast("Resultado liberado. Vamos finalizar sua leitura de performance.");

    if (isMidpoint) {
      renderQuestion();
      return;
    }
    renderProcessing();
  });
}

function renderProcessing() {
  screen.innerHTML = `
    <section class="workspace">
      <div class="quiz-frame">
        <article class="panel">
          <p class="question-label">Inteligência HERO</p>
          <h2>Organizando seus sinais de performance.</h2>
          <p class="support">
            A Inteligência HERO organiza informações, identifica padrões e oferece suporte complementar.
            Todas as decisões importantes continuam conectadas ao acompanhamento humano.
          </p>
          <ul class="processing-list">
            ${processingMessages.map((message) => `<li><span class="pulse"></span>${message}</li>`).join("")}
          </ul>
        </article>
      </div>
    </section>
  `;
  window.setTimeout(async () => {
    state.result = calculateResult(false);
    state.result = await enrichResultWithAi(state.result);
    persistEvent("quiz_completed", state.result);
    renderResult();
  }, 1700);
}

async function enrichResultWithAi(result) {
  try {
    const response = await fetch("/.netlify/functions/diagnosis", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        lead: state.lead,
        profile: state.profile,
        answers: state.answers,
        result,
      }),
    });

    if (!response.ok) return result;

    const ai = await response.json();
    return {
      ...result,
      diagnosis: Array.isArray(ai.diagnosis) && ai.diagnosis.length ? ai.diagnosis.slice(0, 5) : result.diagnosis,
      actions: Array.isArray(ai.actions) && ai.actions.length ? ai.actions.slice(0, 5) : result.actions,
      aiSummary: ai.summary || "",
      aiGenerated: Boolean(ai.aiGenerated),
    };
  } catch {
    return result;
  }
}

function calculateResult(isPreview) {
  const answerList = Object.entries(state.answers)
    .filter(([key]) => key !== "profile")
    .map(([, answer]) => answer);
  const scores = answerList.map((answer) => Number(answer.score || 0));
  const average = scores.length ? Math.round(scores.reduce((sum, score) => sum + score, 0) / scores.length) : 0;
  const penalty = isPreview ? 0 : riskPenalty(answerList);
  const overall = clamp(Math.round(average - penalty), 0, 100);
  const tags = answerList.map((answer) => answer.tag).filter(Boolean);

  const metricScores = {
    "Energia": dimensionScore(["wake_up", "m_energy", "f_energy"], average),
    "Composição corporal": dimensionScore(["body_feeling", "m_composition", "f_body"], average),
    "Força": dimensionScore(["m_strength", "goal_main"], average),
    "Recuperação": dimensionScore(["m_recovery", "f_recovery", "m_power"], average),
    "Consistência": dimensionScore(["routine", "attempts", "days_training", "support_need"], average),
    "Confiança corporal": dimensionScore(["mirror", "m_confidence", "f_selfesteem", "f_identity"], average),
    "Capacidade de evolução": dimensionScore(["blocker", "time_training", "goal_main", "support_need"], average),
  };

  const classification = classify(overall);
  return {
    overall,
    metrics: metricScores,
    classification,
    tags,
    diagnosis: buildDiagnosis(overall, metricScores, tags),
    actions: buildActions(metricScores, tags),
    createdAt: new Date().toISOString(),
  };
}

function dimensionScore(ids, fallback) {
  const values = ids
    .map((id) => state.answers[id]?.score)
    .filter((value) => typeof value === "number");
  if (!values.length) return clamp(Math.round(fallback), 0, 100);
  return clamp(Math.round(values.reduce((sum, score) => sum + score, 0) / values.length), 0, 100);
}

function riskPenalty(answerList) {
  const lowAnswers = answerList.filter((answer) => Number(answer.score) < 35).length;
  return Math.min(12, lowAnswers * 2);
}

function classify(score) {
  if (score >= 82) {
    return {
      title: "Performance em Expansão",
      description:
        "Você já tem bons fundamentos. O próximo salto depende de estratégia, progressão e refinamento do acompanhamento.",
    };
  }
  if (score >= 62) {
    return {
      title: "Performance Instável",
      description:
        "Existe potencial claro, mas sua rotina ainda oscila o suficiente para frear evolução e gerar frustração.",
    };
  }
  if (score >= 42) {
    return {
      title: "Performance Comprometida",
      description:
        "Seu corpo provavelmente não está sem saída; ele está sem um sistema que respeite sua realidade e conduza consistência.",
    };
  }
  return {
    title: "Performance em Alerta",
    description:
      "Os sinais de cansaço, desconexão corporal e baixa continuidade pedem uma intervenção simples, realista e acompanhada.",
  };
}

function buildDiagnosis(score, metrics, tags) {
  const weakest = Object.entries(metrics).sort((a, b) => a[1] - b[1])[0];
  const strongest = Object.entries(metrics).sort((a, b) => b[1] - a[1])[0];
  const blocker = tags[0] || "continuidade";
  return [
    `Identificamos que ${weakest[0].toLowerCase()} é hoje o ponto que mais limita seu Índice HERO, com ${weakest[1]}/100.`,
    `Na vida real, isso costuma aparecer como intenção sem execução, oscilação de energia e sensação de estar sempre recomeçando.`,
    `O principal risco é transformar cansaço em identidade: você passa a funcionar no mínimo e chama isso de normal.`,
    `Seu potencial está em ${strongest[0].toLowerCase()}. Esse é o ponto de apoio para reconstruir rotina sem depender de motivação.`,
    `O próximo passo é um protocolo enxuto, com treino possível, ajustes semanais e acompanhamento humano para remover o bloqueio de ${blocker}.`,
  ];
}

function buildActions(metrics, tags) {
  const weakest = Object.entries(metrics).sort((a, b) => a[1] - b[1]).slice(0, 2).map(([name]) => name);
  const actions = [
    "Estabelecer três sessões semanais de força, mesmo que curtas, com progressão visível.",
    "Definir um horário mínimo de execução para reduzir o intervalo entre intenção e ação.",
    "Organizar alimentação e hidratação primeiro nos dias mais difíceis, não apenas nos dias perfeitos.",
    "Criar uma métrica simples de energia, sono e treino para ajustar o plano sem culpa.",
    "Usar acompanhamento semanal para corrigir rota antes que a rotina vire abandono.",
  ];

  if (weakest.includes("Recuperação")) actions[2] = "Proteger sono, pausa e recuperação como parte do treino, não como prêmio.";
  if (weakest.includes("Confiança corporal")) actions[3] = "Registrar evolução por força, constância e medidas, não apenas por espelho ou comparação.";
  if (tags.includes("tempo")) actions[0] = "Começar com treinos objetivos de 30 a 40 minutos, desenhados para semanas reais.";
  return actions;
}

function renderResult() {
  const result = state.result || calculateResult(false);
  const whatsappText = encodeURIComponent(
    `Olá, time HERO. Fiz meu Check-up e meu Índice foi ${result.overall}/100 (${result.classification.title}). Quero conhecer meu protocolo.`
  );
  screen.innerHTML = `
    <section class="workspace">
      <div class="result-layout">
        <article class="result-card">
          <p class="question-label">Resultado</p>
          <h2>Índice HERO</h2>
          <div class="score-ring" style="--score: ${result.overall}">
            <div>
              <strong>${result.overall}</strong>
              <span>/100</span>
            </div>
          </div>
          <span class="classification">${result.classification.title}</span>
          <p class="support">${result.classification.description}</p>
          <div class="metric-list">
            ${dimensions.map((name) => metricMarkup(name, result.metrics[name])).join("")}
          </div>
        </article>

        <div>
          <article class="result-card">
            <p class="question-label">Diagnóstico com IA</p>
            <h2>${state.lead.firstName ? `${state.lead.firstName}, este é o seu mapa inicial.` : "Este é o seu mapa inicial."}</h2>
            <span class="ai-badge">${result.aiGenerated ? "Análise individual gerada por IA" : "Análise local pronta para IA"}</span>
            <ol class="diagnosis">
              ${result.diagnosis.map((item) => `<li>${item}</li>`).join("")}
            </ol>
            ${result.aiSummary ? `<p class="support">${result.aiSummary}</p>` : ""}
            <p class="micro">
              Este indicador compara seus hábitos, energia, recuperação e consistência com padrões de performance.
              Ele não representa idade biológica ou diagnóstico médico.
            </p>
          </article>

          <article class="result-card" style="margin-top: 18px">
            <p class="question-label">Plano de ação gratuito</p>
            <h2>Comece pelo que destrava execução.</h2>
            <ul class="action-list">
              ${result.actions.map((action) => `<li>${action}</li>`).join("")}
            </ul>
            <div class="offer-band">
              <h3>Você já sabe o que está bloqueando sua evolução. Agora precisa decidir se continuará tentando sozinho.</h3>
              <p>
                O Protocolo HERO transforma as informações do seu Check-up em uma estratégia individual de treino,
                acompanhamento e evolução.
              </p>
              <ul class="benefit-list">
                <li>Protocolo individual adaptado à sua rotina.</li>
                <li>Plataforma própria para acompanhar treino e evolução.</li>
                <li>Suporte humano para ajustes, cobrança e continuidade.</li>
                <li>Estratégia de progressão para energia, força e composição corporal.</li>
              </ul>
              <p class="micro"><strong>WhatsApp direto:</strong> (11) 9 2478-7933</p>
              <div class="cta-row">
                <a class="button primary" href="https://wa.me/${WHATSAPP_NUMBER}?text=${whatsappText}" target="_blank" rel="noreferrer" data-conversion="protocol">QUERO CONHECER MEU PROTOCOLO</a>
                <a class="button secondary" href="https://wa.me/${WHATSAPP_NUMBER}?text=${whatsappText}" target="_blank" rel="noreferrer" data-conversion="whatsapp">FALAR COM O TIME HERO</a>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>
  `;

  document.querySelectorAll("[data-conversion]").forEach((link) => {
    link.addEventListener("click", () => persistEvent(`click_${link.dataset.conversion}`, result));
  });
}

function metricMarkup(name, value) {
  return `
    <div class="metric">
      <div class="metric-head"><span>${name}</span><span>${value}/100</span></div>
      <div class="metric-track"><span style="--metric: ${value}%"></span></div>
    </div>
  `;
}

function renderLegal(type) {
  const pages = {
    privacy: {
      title: "Política de Privacidade",
      body:
        "Coletamos primeiro nome, WhatsApp, e-mail e respostas do check-up para liberar seu diagnóstico, enviar orientações relacionadas à sua performance e melhorar a experiência. Os dados podem ser integrados a ferramentas de CRM, automação e atendimento, sempre com finalidade compatível com a evolução solicitada por você.",
    },
    terms: {
      title: "Termos de Uso",
      body:
        "O Check-up de Performance HERO é uma ferramenta educativa e estratégica. As respostas geram uma análise inicial baseada em padrões de rotina, energia, consistência e percepção corporal. O uso da ferramenta não garante resultados específicos e não substitui acompanhamento profissional individual.",
    },
    health: {
      title: "Aviso de Saúde",
      body:
        "Este check-up não realiza diagnóstico médico, clínico, hormonal, nutricional ou psicológico. Antes de iniciar treinos, mudanças alimentares ou protocolos intensos, procure orientação de profissionais habilitados, especialmente em caso de sintomas, dores, condições pré-existentes ou uso de medicamentos.",
    },
  };
  const page = pages[type];
  screen.innerHTML = `
    <section class="workspace">
      <div class="quiz-frame">
        <article class="panel">
          <p class="question-label">Legal</p>
          <h2>${page.title}</h2>
          <p class="support">${page.body}</p>
          <div class="cta-row">
            <button class="button primary" type="button" id="backHome">Voltar ao início</button>
          </div>
        </article>
      </div>
    </section>
  `;
  document.querySelector("#backHome").addEventListener("click", renderLanding);
}

function renderAdmin() {
  const records = loadRecords();
  const leads = records.filter((record) => record.type === "lead_captured");
  const completed = records.filter((record) => record.type === "quiz_completed");
  const clicks = records.filter((record) => record.type.startsWith("click_"));
  const avg = completed.length
    ? Math.round(completed.reduce((sum, record) => sum + Number(record.payload.overall || 0), 0) / completed.length)
    : 0;

  screen.innerHTML = `
    <section class="workspace">
      <div class="admin-grid">
        <article class="panel">
          <p class="question-label">Admin MVP</p>
          <h2>Leads e conversões locais</h2>
          <p class="support">
            Este painel usa o armazenamento do navegador para validar o fluxo. Na próxima etapa, estes eventos entram em Supabase,
            CRM, WhatsApp API e webhooks.
          </p>
          <div class="stat-grid">
            <div class="stat"><strong>${leads.length}</strong><span>Leads</span></div>
            <div class="stat"><strong>${completed.length}</strong><span>Conclusões</span></div>
            <div class="stat"><strong>${avg}</strong><span>Índice médio</span></div>
            <div class="stat"><strong>${clicks.length}</strong><span>Cliques na oferta</span></div>
          </div>
          <div class="cta-row">
            <button class="button secondary" type="button" id="exportData">Exportar JSON</button>
            <button class="button secondary" type="button" id="clearData">Limpar dados locais</button>
          </div>
        </article>
        <div class="admin-table-wrap" style="margin-top: 18px">
          <table>
            <thead>
              <tr>
                <th>Data</th>
                <th>Evento</th>
                <th>Lead</th>
                <th>Perfil</th>
                <th>Índice</th>
                <th>Tags</th>
              </tr>
            </thead>
            <tbody>
              ${records.length ? records.slice().reverse().map(recordRow).join("") : `<tr><td colspan="6">Nenhum evento registrado ainda.</td></tr>`}
            </tbody>
          </table>
        </div>
      </div>
    </section>
  `;

  document.querySelector("#exportData").addEventListener("click", () => {
    const blob = new Blob([JSON.stringify(records, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const anchor = document.createElement("a");
    anchor.href = url;
    anchor.download = "hero-checkup-leads.json";
    anchor.click();
    URL.revokeObjectURL(url);
  });

  document.querySelector("#clearData").addEventListener("click", () => {
    localStorage.removeItem(STORAGE_KEY);
    showToast("Dados locais apagados.");
    renderAdmin();
  });
}

function recordRow(record) {
  const payload = record.payload || {};
  const lead = record.lead || {};
  return `
    <tr>
      <td>${new Date(record.createdAt).toLocaleString("pt-BR")}</td>
      <td>${record.type}</td>
      <td>${lead.firstName || "-"}<br><span class="micro">${lead.email || ""}</span></td>
      <td>${record.profile || "-"}</td>
      <td>${payload.overall ?? "-"}</td>
      <td>${(payload.tags || []).join(", ") || "-"}</td>
    </tr>
  `;
}

function persistEvent(type, payload) {
  const record = {
    type,
    createdAt: new Date().toISOString(),
    profile: state.profile,
    lead: state.lead,
    answers: state.answers,
    payload,
  };
  const records = loadRecords();
  records.push(record);
  localStorage.setItem(STORAGE_KEY, JSON.stringify(records));

  // Future integrations: send this same record to Supabase, CRM, WhatsApp API or a webhook.
  if (integrations.webhookUrl) {
    fetch(integrations.webhookUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(record),
    }).catch(() => {});
  }
}

function loadRecords() {
  try {
    return JSON.parse(localStorage.getItem(STORAGE_KEY) || "[]");
  } catch {
    return [];
  }
}

function resetToLanding() {
  state.phase = "landing";
  renderLanding();
}

function showToast(message) {
  toast.textContent = message;
  toast.classList.add("visible");
  window.clearTimeout(showToast.timer);
  showToast.timer = window.setTimeout(() => toast.classList.remove("visible"), 2600);
}

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

boot();
